import React from 'react'
import ReactDOM from 'react-dom/client'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <h1 style={{ color: 'red', fontSize: '48px', margin: '50px', textAlign: 'center' }}>
      Test Successful!
    </h1>
  </React.StrictMode>,
)
